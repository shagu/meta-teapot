meta-teapot
===========

Teapot is an openembedded/yocto layer for serveral arm boards.
The layer is focused on providing a working mainline kernel and u-boot for theese architectures.
If a feature-complete-linux is required, you should not use layer and fallback to the vendor kernels 
provided by other layers.

buildchain
==========

Checkout the oe base repositories:
	git clone git://git.openembedded.org/openembedded-core oe-core
	cd oe-core
	git clone git://git.openembedded.org/bitbake bitbake

Checkout the huge oe package repository:
	git clone git://git.openembedded.org/meta-openembedded

Checkout the teapot repository:
	git clone https://github.com/shagu/meta-teapot.git

init build / generate builddir:
	source ./oe-init-build-env

configure your local.conf (conf/local.conf):
	DISTRO ?= "teapot"

configure your bblayers.conf (conf/bblayers.conf):
	LCONF_VERSION = "5"

	BBPATH = "${TOPDIR}"
	BBFILES ?= ""

	BBLAYERS ?= " \
	  ${TOPDIR}/../meta \
	  ${TOPDIR}/../meta-teapot \
	  ${TOPDIR}/../meta-openembedded/meta-oe \
	  "

	BBLAYERS_NON_REMOVABLE ?= " \
	  ${TOPDIR}/../meta \
	  "

build your first image for default target:
	bitbake base-image

or for ecafe target:
	MACHINE=ecafe bitbake base-image
